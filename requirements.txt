# HX Stomp Soundboard dependencies
#
# mido     - Pure-Python MIDI library (message construction, port I/O)
# python-rtmidi - Low-latency MIDI I/O backend used by mido
#
# Install:
#   pip install -r requirements.txt
#
# On Linux you may also need:
#   sudo apt install libasound2-dev libjack-dev   # for rtmidi to build

mido>=1.3.0
python-rtmidi>=1.5.0
customtkinter>=5.2.0
Pillow>=10.0.0              # required by customtkinter for CTkImage icon rendering
anthropic>=0.40.0           # Anthropic Claude (default provider)
openai>=1.0.0               # OpenAI GPT provider
google-generativeai>=0.8.0  # Google Gemini provider
# (Ollama uses stdlib urllib — no extra dep)
